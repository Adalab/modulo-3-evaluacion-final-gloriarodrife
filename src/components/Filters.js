import FilterMovie from './FilterMovie';
import FilterYear from './FilterYear';

const Filters = () => {
  return (
    <>
      <FilterMovie />
      <FilterYear />
    </>
  );
};

export default Filters;
